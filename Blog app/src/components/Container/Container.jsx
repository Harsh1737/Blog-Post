import React from "react";

const container = ({ childern }) => {
  return <div className="w-full max-w-7xl mx-auto px-4">{childern} </div>;
  
  // 1 line return doesnt require ( )
};

export default container;
